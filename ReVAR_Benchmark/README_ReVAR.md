Steps:

1. Install the required dependencies as specified.
2. Load the datasets from the file load_datasets_revar.ipynb.
3. Run the file ReVar.ipynb. Ensure that the required dependencies are present in the same directory as the two files mentioned above.